#pragma once
#include <windows.h>

class Input {
public:
  /// <summary>
  /// 初期化
  /// </summary>

  void Initialize(HINSTANCE hInstance, HWND hwnd);
  /// <summary>
  // 更新
  /// </summary>
  void Update();

private:
  /// directInput

  /// keyboardDevice

  /// 各キーの入力状態

  // iframe前の各キーの入力状態
};
